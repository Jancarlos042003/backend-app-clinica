spring:
  application:
    name: postalta-api
  profiles:
    active: dev
  jackson:
    serialization:
      INDENT_OUTPUT: true

server:
  port: 8080

logging:
  level:
    root: INFO
    org.springframework.web: DEBUG
